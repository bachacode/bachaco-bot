{
    "$schema": "https://turbo.build/schema.json",
    "globalDependencies": ["**/.env"],
    "globalEnv": ["NODE_ENV"],
    "tasks": {
        "build": {
            "dependsOn": ["^build"]
        },
        "dev": {
            "persistent": true,
            "cache": false
        },
        "lint": {}
    }
}
